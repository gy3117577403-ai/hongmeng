import Link from 'next/link';
import { ArrowUpRight, ClipboardCheck, ScanLine, Wrench } from 'lucide-react';
export default function QualityScanEntry({code,name,canQuick=false,canQuality=true,canReport=true}:{code:string;name:string;canQuick?:boolean;canQuality?:boolean;canReport?:boolean}) {
  return <main className="qd-scan-entry"><header><span className="qd-scan-mark"><ScanLine size={24}/></span><div><b>工单现场操作</b><small>{name} · 已登录</small></div></header><div className="qd-scan-welcome"><small>选择本次操作</small><h1>工单现场，一码进入</h1><p>所有入口使用当前工单，进入后仍可切换。</p></div>{canReport&&<Link className="qd-entry-card" href={'/field-report/'+encodeURIComponent(code)+'?mode=report'}><Wrench size={30}/><div><h2>生产报工</h2><p>登记工序进度、数量与工时</p></div><ArrowUpRight size={22}/></Link>}{canQuality&&<Link className="qd-entry-card quality" href={'/quality-capture/'+encodeURIComponent(code)}><ClipboardCheck size={30}/><div><h2>质量登记</h2><p>压检、拉力、成品、导通与首件检验</p></div><ArrowUpRight size={22}/></Link>}{canQuick&&<Link className="qd-entry-card quality" href={'/quality-quick-capture/'+encodeURIComponent(code)}><ClipboardCheck size={30}/><div><h2>异常快处</h2><p>记录现场问题，按需发布警示</p></div><ArrowUpRight size={22}/></Link>}<p className="qd-scan-foot">检验记录随订单批次归档</p></main>;
}
