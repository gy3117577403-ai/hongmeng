# 成品仓实施与验收

用户已选择：第一版紧凑列表为默认页面，第三版连续处理栏，第二版按日发货批次分组。目标 1366×1024，紧凑模式一屏 24 条。基于已发布 v1.34.173；计划版本 v1.34.175，完成后发布、验证杭州镜像，不切换生产 Sealos。

## 数据规则

- 有效 FINISHED_GOOD 流水是生产入库来源；数据库事务生成待接收记录，不靠打开页面触发，不从计划数量推算新库存。
- 待接收 P 在实物库存之外；实物 Q = 可用 F + 占用 R + 留库 H + 隔离 B。每次变动保留流水，事务锁与幂等键防重复；来源撤销不得吞掉已收/已发库存。
- 急单接收并发货是一个原子操作、两条实物记录；留库到期只提醒，不自动释放。
- 成品仓是实际发货唯一写入口；日计划保留需求和历史。必要的日计划 ShipmentEvent 为成品仓同事务生成的兼容投影，带唯一来源，禁止旧 API 发货/冲销。
- 发货单与生产批次独立；多行合单须同客户和收货目的地；保存单号不发货；快递缺单号允许发货但列入待补；自提须交接人。
- 退货先隔离；返工转出/回库独立于报工；报废和盘点须原因。附件在 S3，删除为软删除。
- 历史有效完工减旧净发货仅作期初待核对候选；人工核对后才收货。历史物流信息不伪造。
- 成品仓向所有已登录用户开放，不新增角色或审批。

## 验收待办

- [x] Prisma 模型、迁移、自动待接收、期初核对和来源撤销保护。
- [x] 库存/接收/留库/草稿/占用/发货/物流/批次/退货/返工/调整/附件/真实导出。
- [x] 紧凑列表、连续处理、批次视图、分页/搜索/筛选、导航与旧入口关闭。
- [ ] 关键并发和事务回滚集成测试、全量规定检查、隔离环境 HTTP 与浏览器验收。
- [x] 与选中图片同尺寸对比，design-qa.md passed。
- [ ] 发布固定镜像、CI 验证、杭州匿名拉取与独立运行，提供完整镜像地址和 digest。
