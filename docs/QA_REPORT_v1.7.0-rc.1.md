# v1.7.0-rc.1 本地验收报告

## 版本信息

- 版本：v1.7.0-rc.1
- 验收日期：2026-07-02
- 验收范围：本地开发环境、Docker Compose、本地 PostgreSQL、本地 MinIO
- 部署状态：未部署 Sealos，未修改 Sealos 环境变量，未检查 GHCR digest

## 功能范围

- 工单列表横向抽屉
- 资料库默认全屏显示
- 顶部当前工单胶囊
- 状态统一为未发图 / 在前端 / 在后端 / 已完成
- 工单状态、优先级、计划时间快捷修改
- 工单计划时间字段
- CSV 导入导出计划时间和状态
- 元数据导出计划时间
- 打印摘要计划时间
- 工单卡片紧凑化
- 资料完整性显示为完整 / 缺资料 N / 无资料

## 数据库验证

- `npx prisma generate`：通过
- `npx prisma migrate deploy`：通过
- 新增 migration：`202607020001_work_order_planned_at`
- 新增字段：`work_orders.planned_at`
- 迁移策略：仅新增可空字段，不删除已有字段，不破坏历史数据

## 构建验证

- `npm run build`：通过
- `docker build -t hongmeng-workorder:v1-7 .`：通过
- `docker compose -p hongmeng up --build -d`：通过
- `npm run smoke`：通过
- `/api/health`：返回 `ok: true`

构建过程中仍有 Next.js 对 `<img>` 标签的性能建议，不影响本次 RC 验收。

## API 回归

- 登录：通过
- 当前用户接口：通过
- 工单新增：通过
- 工单编辑：通过
- 工单状态修改：通过
- 工单优先级修改：通过
- 工单计划时间修改：通过
- 工单关键词 + 状态组合筛选：通过
- 全局搜索：通过
- 工单软删除：通过
- 回收站显示软删除工单：通过
- CSV 导入状态和计划时间：通过
- CSV 导出状态和计划时间：通过
- metadata JSON 导出计划时间：通过
- 操作日志加载：通过
- 操作日志新增动作：通过
- 操作日志敏感信息摘要检查：通过

## 文件回归

- 上传 PDF：通过
- 上传图片：通过
- 文件信息编辑：通过
- 文件跨分类移动：通过
- 单文件预览：通过
- 单文件下载：通过
- 当前工单 ZIP 下载：通过
- ZIP 内部分类目录：通过
- 文件软删除：通过
- 回收站显示软删除文件：通过
- 文件恢复：通过

## 账号与密码回归

- 账号新增：通过
- 临时账号登录：通过
- 修改密码：通过
- 旧密码失效：通过
- 新密码可登录：通过
- 本地调试账号密码已恢复，仅限本地环境

## 平板视口验证

- 1366x1024：通过，无横向溢出，抽屉宽度 410px
- 1280x800：通过，无横向溢出，抽屉宽度 384px
- 1024x768：通过，无横向溢出，抽屉宽度 360px

## 抽屉交互验证

- 默认抽屉关闭：通过
- 点击“切换工单”打开抽屉：通过
- 点击遮罩关闭抽屉：通过
- 屏幕左缘向右滑动打开抽屉：通过
- 抽屉内向左滑动关闭抽屉：通过
- 右侧资料区保持挂载：通过

## 页面文案验证

页面可见文案不再展示：
- 待处理
- 进行中
- 空工单
- pending
- processing
- completed
- frontend
- backend

## 浏览器验证

- dashboard console error：0
- dashboard console warning：0
- 登录页 console error：0
- 抽屉打开 / 关闭后 console error：0

## 已修复问题

- 修复工单列表接口在关键词和状态筛选同时传入时查询条件互相覆盖的问题。
- 修复计划时间在服务端 UTC 和浏览器本地时区之间产生 hydration mismatch 的问题，统一按 `Asia/Shanghai` 展示和解析无时区输入。

## 已知问题

- 暂无阻塞问题。
- 非阻塞提示：构建时仍有 Next.js 对 `<img>` 的性能建议，属于历史提示，不影响本次 v1.7.0-rc.1 本地验收。
