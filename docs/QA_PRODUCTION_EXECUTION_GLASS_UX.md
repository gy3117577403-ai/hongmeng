# v1.16.4 生产执行玻璃工作台 QA 报告

日期：2026-07-11
状态：本地验证通过，未部署 Sealos

## 环境

- Windows 本地 Docker Compose：Next.js、PostgreSQL、MinIO。
- 视口：1366x1024、1280x800、1024x768。
- 压力数据：Playwright 网络桩生成 200 条只读工单，四列各 50 条；不写 PostgreSQL，不访问或修改 S3。
- 截图目录：`output/playwright/`（已忽略，不提交）。

## 已通过检查

| 检查项 | 结果 |
| --- | --- |
| 200 卡片渲染 | 通过，200 条完整渲染 |
| 四列可滚动高度 | 通过，1366 视口每列 736px，可滚动内容约 9,500px |
| 鼠标滚轮独立列滚动 | 通过，目标列 `0 → 700`，相邻列保持 `0`，页面保持 `scrollY=0` |
| 1024 横向滚动 | 通过，看板 `clientWidth=998`、`scrollWidth=1236`，页面无横向溢出 |
| 列标题 | 通过，固定在各列顶部 |
| 默认复选框 | 通过，批量模式前 0 个，进入后 200 个，退出后恢复 0 个 |
| 负责人 / 工位 / 我的任务 | 通过，生产执行页面文本中均不存在 |
| 外部点击关闭菜单 | 通过，管理员与状态菜单均关闭 |
| Escape 与焦点返回 | 通过 |
| 单菜单互斥 | 通过，打开高级筛选后管理员菜单关闭 |
| 客户筛选与 URL | 通过，测试客户从 200 条筛到 25 条，并生成 Filter Chip |
| 搜索防抖与 URL | 通过，`QA-0002` 从 200 条筛到 1 条 |
| 摘要点击筛选 | 通过，紧急摘要从 200 条筛到 16 条，再次点击恢复 |
| 今日任务去重 | 通过，快捷项为今日交期 / 已逾期 / 今日更新 / 今日完成 |
| 状态局部更新 | 通过，成功后卡片移动到新状态列，未整页刷新 |
| 更新失败回滚 | 通过，模拟 500 后卡片恢复原列并显示可读错误 |
| 工单资料直达 | 通过，Dashboard URL 包含工单、原图分类、来源和 `returnKey` |
| 返回滚动恢复 | 通过，横向 `180 → 180`、未发图列 `760 → 760`，恢复后会话键清理 |
| 页面级横向溢出 | 三个视口均无 |
| WebView 视觉降级 | 浏览器标识模拟通过，列容器 `backdrop-filter: none`、背景不透明度 `.94` |
| Compose 构建与健康检查 | 通过，`/api/health` 返回 `ok=true` |
| 浏览器 Console | 通过，最终重载后 0 error、0 warning |

## 返回状态专项

跳转 URL 已确认包含 `workOrderId`、`categoryCode=drawing`、`from=production` 和随机 `returnKey`。会话状态正确保存看板横向位置与四列纵向位置。针对 Next Router 复用缓存页面的行为，生产页和 Dashboard 共同执行写入后校验及短间隔重试；最终镜像自动化确认横向位置 `180`、未发图列位置 `760` 均精确恢复，随后删除对应会话记录。

## 筛选和导出

- 列表与 CSV 共用服务端筛选解析器。
- 客户选项来自当前周全量工单。
- 已覆盖客户、交期预设与范围、状态、优先级、图纸、配料和资料完整度参数。
- CSV 已移除负责人和工位列。
- 直接请求本地 API 验证有效组合与 CSV 均返回 200；交期开始晚于结束时返回明确 400。

## 回归边界

Next.js 生产构建通过。已登录会话直接请求生产工单、周计划、图纸资料库、连接器参数和组装说明书页面均返回 200。共享 PortalMenu 只接入菜单关闭能力，未修改连接器说明书 PDF 缩放、目录或手势逻辑。未新增 Prisma migration。

## 最终构建与限制

- `npx prisma generate`、`npm run build`、`npm run smoke` 均通过。
- `docker build -t hongmeng-workorder:v1-16-4-production-glass .` 通过，最终容器健康检查通过。
- 最终容器已重跑返回位置、200 卡片、三个视口与 console error 检查。
- Edge 与真实 Android WebView APK 本轮没有独立真机环境；将记录为设备侧复验项，不虚报通过。WebView 高不透明度降级已通过浏览器标识模拟。
