# v1.31.1 发布说明

候选镜像：`ghcr.io/gy3117577403-ai/hongmeng:v1.31.1`

## 修复内容

- 工艺审批、启用重复提交改为语义幂等；数据库已经推进状态时，不再返回笼统的 `当前状态不能执行 approve`。
- 状态或版本冲突会返回数据库当前状态和版本，审核界面自动重新读取并显示真实结果。
- 流程中心、工艺变更收件箱及同浏览器其他标签页同步刷新，避免两个审核面板状态不同步。
- 审批按钮增加同步提交锁，重复点击复用同一个命令幂等键。
- 企业微信即时通知调度失败不再把已经提交成功的审批或启用返回为业务失败；待发消息继续由可靠出站任务重试。

## 验收边界

- 本补丁不新增 Prisma 迁移，不修改现有工艺、报工或工时数据。
- 发布镜像不代表已切换 Sealos 生产环境；生产切换需另行确认当前镜像、数据库备份和目标不可变 digest。
