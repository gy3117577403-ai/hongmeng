# v1.10.0-rc.4 连接器参数页查询体验优化验收报告

## 基本信息

- 版本：v1.10.0-rc.4 查询体验优化
- 功能 commit：936dd2097054a80d59215e4fbb4151c80ed19d62
- 状态：本地验证通过，等待统一部署验收
- 是否部署 Sealos：否
- 是否检查 GHCR digest：否
- 是否修改线上环境变量：否
- 是否新增 Prisma migration：否
- 是否新增依赖：否
- 截图目录：`C:\Windows\Temp\hongmeng-v110-query-polish-screenshots`

## 查询体验优化验收

- 顶部统计区已压缩为一行紧凑统计条。
- 筛选按钮保持一行展示。
- 表格区域明显上移，优先占用页面高度。
- 1366x1024：表格起点约为页面高度 24.5%，无页面级横向溢出。
- 1280x800：表格起点约为页面高度 22.3%，无页面级横向溢出。
- 1024x768：表格起点约为页面高度 23.2%，满足顶部区域不超过 25% 的要求。
- 浏览器 console error：0。

## 附件抽屉验收

- 原始资料附件默认收起。
- 页面右侧提供“原始资料”按钮。
- 点击后右侧覆盖式展开 320px 抽屉。
- 1024 宽度下抽屉覆盖表格，不挤压表格布局。
- 抽屉内保留上传、附件列表、下载、删除能力。
- 打开 / 关闭抽屉未触发连接器列表 API 重新加载。

## 操作列简化验收

- 表格每行操作列只显示“编辑 / 更多”。
- “更多”菜单包含标记重点 / 取消重点、复制整行参数和删除。
- 删除仍走二次确认。
- 操作按钮高度适合平板触控。

## 重点列优化验收

- 非重点行重点列为空白，不再显示“否”。
- 重点行显示黄色“重点”标签。
- 重点行保留浅黄色整行背景。
- 重点筛选只显示重点行。
- 批量标记重点 / 批量取消重点回归通过。

## 缺失字段筛选高亮验收

- “缺外剥皮”筛选下，外剥皮为空的单元格显示浅橙底。
- “缺内剥皮”筛选下，内剥皮为空的单元格显示浅橙底。
- “缺入长”筛选下，入长为空的单元格显示浅橙底。
- “任意缺失”筛选下，所有缺失参数单元格显示浅橙底。
- “全部”状态下空值保持干净空白。

## 搜索高亮验收

- 搜索框 placeholder 为“搜索型号 / 参数 / 备注”。
- 搜索结果显示“查询结果 N 条”。
- 型号、外剥皮、内剥皮、入长和备注命中内容使用浅黄色背景高亮。
- 测试搜索 `CSV` 后命中高亮 6 处，结果行 3 条。
- 清空搜索按钮可见。
- 防抖查询保留，回车可立即触发查询。

## 复制整行参数验收

- “更多”菜单提供“复制整行参数”。
- 复制格式包含型号、外剥皮、内剥皮、入长和备注。
- 空值保持为空，不写成 0。
- 成功提示“已复制参数”。
- 复制动作写入 operation_logs，action 为 `copy_connector_parameter`。
- 日志不记录密码、数据库连接串、对象存储密钥或会话密钥。

## 回归测试结果

- 新增参数：通过。
- 编辑参数：通过。
- 删除 / 恢复：通过。
- 导入预览 / 确认导入：通过。
- CSV / XLSX / 粘贴导入：通过。
- CSV 导出：通过。
- 模板下载：通过。
- 原始资料上传 / 下载 / 删除：通过。
- 批量标记重点 / 取消重点 / 删除：通过。
- 搜索和筛选：通过。
- 生产资料库上传 / 预览 / 下载 / ZIP：通过。
- 登录和修改密码：通过。

## 本地构建验证

- `npx prisma generate`：通过。
- `npx prisma migrate deploy`：通过，无待执行 migration。
- `npm run build`：通过。
- `docker build -t hongmeng-workorder:v1-10-query-polish .`：通过。
- `docker compose -p hongmeng up --build -d`：通过。
- `npm run smoke`：通过。
- `/api/health`：返回 `ok: true`。

## 已知问题

- `v1.10.0-rc.3` tag 已存在并指向旧候选 commit，本次固化不覆盖该 tag。
- 暂无阻塞问题。
