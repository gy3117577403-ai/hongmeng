# v1.34.107 计划工艺关联与量产批量导入总镜像

本版本是任务一“计划工艺关联显示”和任务二“量产计划图纸库安全批量导入”的总镜像，完整业务说明见 `v1.34.106-planning-import.md`。

## 相对 v1.34.106 的发布修正

- `v1.34.106` 的 GitHub 作业在 Unit tests 阶段停止，镜像构建和推送均未执行，不能作为可用镜像。
- 失败原因是旧的计划产品幂等测试仍模拟单条 `findFirst` 查询，未模拟新版候选集合 `findMany` 查询；业务代码、Prisma 迁移和前置 PostgreSQL 集成步骤并未在该次作业中失败。
- 本版本补齐旧测试桩，继续验证“重复创建幂等、删除恢复需确认”的原业务规则，没有放宽产品匹配或图纸库保护条件。
- 因发布标签不可变，保留失败的 `v1.34.106` 作为审计记录，使用新标签 `v1.34.107` 重新执行完整发布流水线。

## 上线边界

- 本次只发布总镜像，不自动切换 Sealos 生产实例。
- 上线时会应用新增的 `ProductionPlanImportBatch` 表迁移；不会删除或覆盖已有图纸、SOP、正式工序工时和订单关联资料。
