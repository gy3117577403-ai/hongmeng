# v1.34.152 发布验收

最终应用源码提交为 `ac4bc8d5f5c918e223f88f5908d6fb331c3defa5`，固定标签为 `v1.34.152`。

```
crpi-2acb2dabuutklbx4.cn-hangzhou.personal.cr.aliyuncs.com/zhiju-b/hongmeng:v1.34.152
```

镜像摘要为 `sha256:2478543596679700314b924043c4ea5dc143e6b600f4e243a0d3b1763923bf8f`，平台 `linux/amd64`。[GitHub 发布流程 34430158989](https://github.com/gy3117577403-ai/hongmeng/actions/runs/34430158989) 完成且通过。

## 下载和运行

- 杭州仓库与 GHCR 清单摘要一致。使用不含登录凭据的 Docker 配置拉取成功，并匿名下载全部镜像配置和层，校验 182,830,339 字节内容的摘要与大小，无重试。
- 实际杭州镜像以摘要固定启动于全新 PostgreSQL 数据库和独立 S3 桶。147 个 Prisma 迁移完成，0 个未完成；健康、数据库、存储就绪检查通过。
- 重新启动应用容器后，版本、报表和已通过时长保持一致。重启前后通过浏览器下载的台账和处理轨迹两张工作表内容完全一致。

## 业务和页面

- PostgreSQL 其他工时集成用例 8 项通过，包含不完整草稿、照片及旧字段保留、提交校验、状态汇总、更正筛选交集。完整发布流程同时覆盖员工调岗、工时、日报及生产等既有回归用例。
- 实际镜像 HTTP 验收 9 组通过：五类账号登录、权限范围、幂等及来源约束、实际 S3 照片读写、并发审批一次、按原工作日入账、日周月与个人矩阵一致、Excel 实际下载、更正作废回退、退回重提及核减审批。
- 实际镜像浏览器从扫码入口登录员工账号，空表单不能提交；先上传照片再补齐时长和说明，成功提交 60 分钟协助样品和 30 分钟临时安排。组长连续通过两笔，第一笔结束后自动选中下一笔，最终显示待办已处理完。
- 个人日报收到 90 分钟其他工时，实际出勤 8 小时、目标工时 7.6 小时，达成率为 19.74%；重启后完全一致。
- 手机表单及填写说明均无达成率公式，320 和 390 像素宽度下操作栏无横向溢出；桌面 1366×768 和 1366×1024 可查看和操作。桌面及手机实际镜像页面控制台均为 0 错误、0 警告。
- 开发阶段另验证了 32 条草稿从首屏 30 条继续加载、打开详情后恢复列表位置和已加载记录、日期状态搜索组合筛选、撤回重提、未保存内容保存后换账号，以及断网后照片重试。界面无常驻成功通知，历史非空补充字段折叠展示。
- 固定二维码在实际镜像中生成并下载成功，入口仍为 `/field-report/other-hours`。

浏览器验收使用桌面 Chromium 和手机尺寸模拟；未连接真实微信客户端检查物理键盘、相机权限或扫码设备行为。所有申报与审批操作均在隔离验收环境完成。

## 部署

Sealos 更新镜像栏时复制上面的完整地址，容器端口继续为 `3000`，沿用现有数据库、对象存储和环境变量。启动入口会执行新增迁移，不需要重新生成或张贴现有二维码。

本次交付已发布并验证的镜像，未切换正式 Sealos 应用，也未修改正式员工、申报或审批数据。机器验收摘要见同目录 `v1.34.152-verification.json`。
