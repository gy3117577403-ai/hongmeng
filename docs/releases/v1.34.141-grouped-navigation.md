# v1.34.141 — grouped platform navigation

The platform sidebar previously exposed a long flat list whose typography was affected by the active page. A shared navigation component now groups the existing entries into Production and Planning, Quality, Materials and Warehousing, Technology and Documents, People and Labor, and Collaboration and Approvals. Home and Reports remain direct entries; System Management and the planned Help entry sit in the footer.

Only one business group opens at a time. Parent rows expand without navigating; child rows keep the existing destinations and access filtering. A compact icon rail opens click-operated flyouts with viewport clamping, outside-click/focus dismissal, Escape and focus return. Expanded group preferences are stored per user, preserve an explicitly closed group on same-route reload, and resolve the owning group after a route change. The rail itself retains the existing initially compact behavior.

Navigation owns its font, row geometry, icons and light/orange state colors across workbenches. Desktop expanded width is 264px, compact width is 72px, and narrower screens use a 64px rail plus an overlay. Child targets are at least 44px high. Long menus scroll independently between the brand and fixed footer. Reduced-motion users receive no menu animation.

HR now mounts the same platform navigation and exposes a trigger beside its existing internal tabs. The employee directory's persistent Save/Cancel footer remains visible. Existing production/planning/warehouse business-mode controls and sample URLs remain available. No database schema or access-policy changes are included.

## Validation before publication

- TypeScript and lint passed; lint retains existing image-element warnings outside this change.
- Full unit suite: 1,305 tests, 1,144 passed, 161 environment-dependent skipped, zero failures.
- Dedicated PostgreSQL/MinIO fixtures: all 32 manifest destinations, the report entry's canonical redirect, five deep/sample links and restricted-reader visibility/HR redirect passed (44 HTTP checks).
- Browser verification: real quality/plan/HR navigation; one open group; same-route preference restoration; compact flyout dismissal/focus return; 1366×1024 desktop and 1366×768 short screen; 390×844 overlay and 320×640 flyout containment. HR edit footer bottom was 751.33px in a 768px-high viewport with the global sidebar both compact and expanded.
- Only synthetic local records were used. Physical tablets were not tested.

Exact source identity, CI completion, anonymous mirror integrity and fresh image runtime evidence are recorded separately after publication. Production/Sealos is not switched by this release task.

HR navigation is guarded while an employee draft is unsaved. Both compact and expanded sidebar links and Ctrl+K wait for Continue Editing / Discard; continuing preserves input, and discarding reaches the selected destination. Concurrent navigation requests share one confirmation. All 1,305 unit tests were rerun after this change with 1,144 passed, 161 skipped and zero failures; TypeScript, lint and all 44 local HTTP checks also passed again.

The initial v1.34.140 workflow was deliberately cancelled before image publication to include this protection. Its tag was not moved. The bounded local fixture names and evidence directory retain the task identifier navigation-v134140; runtime version/revision assertions require v1.34.141.

## Published image acceptance

- Source: `da8383409905fd560cb045ffd9f519bc5281f218`; tag `v1.34.141`.
- CI: https://github.com/gy3117577403-ai/hongmeng/actions/runs/34306199396 — completed successfully, including exact-image smoke and anonymous China mirror pull with a clean sample runtime.
- Image: `ghcr.dockerproxy.net/gy3117577403-ai/hongmeng:v1.34.141`.
- Immutable digest: `sha256:ba57d9a0da18667b5d2e5ca23c72b688b2ba4d1508dd877ba192579f9cb56ca6`, `linux/amd64`.
- Independent anonymous verification matched the upstream/mirror manifests, OCI version/revision and every blob's byte length and SHA-256 (182,631,617 bytes total).
- An anonymous Docker pull by digest succeeded. The local acceptance database had zero public tables before startup; the image applied all 141 existing migrations and connected to its dedicated MinIO bucket. Health and readiness returned true with the expected version and full source revision.
- All 47 HTTP checks passed: fresh bootstrap login requires a password change, change and re-login work; all 32 menu routes, report redirect, five deep/sample URLs and restricted-reader checks remain functional.
- Browser against the pulled image: login; quality selection; per-user group restoration after reload; compact flyout Escape and focus return; 1366×1024 desktop; 1366×768 HR footer at y=751.33; 390×844 CSS viewport overlay; HR save/reload/restore; unsaved Continue/Discard navigation. No browser errors were recorded. The fixture name was independently checked as restored.
- Final screenshots: `artifacts/navigation-v134140/release-home-1366x1024.jpg`, `release-quality-1366x1024.jpg`, `release-hr-1366x768.jpg` and `release-mobile-native.jpg`. Desktop files match their requested dimensions. The narrow native capture is 375×812 pixels while the DOM viewport was 390×844; it is retained without resizing and is not physical-device proof.
- Evidence: `ci-v141.json`, `mirror-verification.json`, `release-container-verification.json`, `release-runtime-smoke.json` and `release-browser-acceptance.json` in the same evidence directory.
- Actual-image preview: http://127.0.0.1:3111/home with synthetic data. Production/Sealos and formal business data were not changed.
