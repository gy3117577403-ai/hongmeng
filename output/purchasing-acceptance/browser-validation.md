# 采购模块浏览器验收 · 2026-09-18

验收环境为本机独立数据库 `hongmeng_purchasing_qa`、独立 MinIO 和端口 3190，未操作正式业务环境。

通过 Codex 浏览器实操的记录：`CG2026091800009-01`，物品「扭力螺丝刀 · 浏览器验收」，数量 2，预算及采购总额 360 元。

已完成以下实际操作：

1. 填写并提交采购申请，详情显示相同数量、用途和金额。
2. 通过采购审批，填写供应商、收款人、银行和账户，登记采购。
3. 单独申请 360 元资金，生成 `FK2026091800008`；资金审批通过后，财务登记付款账户与流水。资金单显示已付 360、待付 0。
4. 收货 2 件进入「采购物资仓 / 待上架区」，产生库存编号 `WZ2026091800010` 和收货流水。
5. 上传真实 PDF 文件，登记发票 `UI-INVOICE-0009`，发票关联 360 元；详情自动显示「已完成」。
6. 生成 `UI-CONTRACT-0009` 第 1 版，打开合同存档页并下载 PDF。实际下载文件通过 `pdf-lib` 读取，1 页，压缩后 137013 字节。
7. 1366×1024 下，页面 scrollWidth=1366、scrollHeight=1024，无页面横向溢出；表格在工作台内部滚动，分页始终可见。

验收中发现并修复了导航按钮嵌套、平台全局样式覆盖采购边距，以及合同内联样式导致的首次加载渲染不一致。修复后的 DOM 无嵌套按钮。合同 PDF 下载的浏览器事件等待未返回，但下载目录中的实际文件及 PDF 解析结果确认下载成功。

完整接口验收另见 `local-http.json`；分次付款、部分审批、供应商退货、个人退款归还、红冲及并发校验另由数据库和 HTTP 集成验收覆盖。
